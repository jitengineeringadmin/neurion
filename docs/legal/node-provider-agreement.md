> **DRAFT — not legal advice.** Generated skeleton for legal review (Agent 12). Reflects controls implemented in code.

# Node Provider Agreement

By connecting a Neurion Node you agree:
- to run only allowlisted neurion/* worker images (no arbitrary code);
- to **not retain, read or exfiltrate** any job input/output (ephemeral, memory-only context for chat);
- to honest compute — cheating triggers reputation slash, suspension and clawback;
- to resource/thermal limits enforced by the agent;
- that rewards require verified useful compute and may be withheld for compliance;
- that acceptance is recorded (version + timestamp) and required before serving chat.
