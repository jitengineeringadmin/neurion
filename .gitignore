# --- secrets -----------------------------------------------------------
.env
.env.*
!.env.example
**/.env.local

# --- dependencies ------------------------------------------------------
node_modules/

# --- build output ------------------------------------------------------
dist/
.next/
*.tsbuildinfo
packages/contracts/artifacts/
packages/contracts/cache/
packages/contracts/typechain-types/
apps/node-agent/bin/

# --- desktop packaging (regenerable, ~5 GB) ----------------------------
apps/desktop/staging/
apps/desktop/dist-installer/
apps/desktop/dist-installer-*/

# --- caches ------------------------------------------------------------
.turbo/
**/.turbo/

# --- local runtime state ----------------------------------------------
.runtime/
.local-run/
.local-data/
.tmp-*
**/.tmp-*

# --- logs / os ---------------------------------------------------------
*.log
Thumbs.db
.DS_Store
dist-bundle

# Release signing key — must never enter the repository.
# If this is ever lost, no installed copy can be updated again.
release-key.json
.neurion/
